/* =========================================
   DANAKITA - GLOBAL STYLES
   ========================================= */

:root {
  --tk-primary: #00A6ED;
  --tk-primary-dark: #0089C9;
  --tk-secondary: #FF6B35;
  --tk-secondary-dark: #E55A2B;
  --tk-success: #00B894;
  --tk-danger: #FF4757;
  --tk-bg: #F8F9FA;
  --tk-card: #FFFFFF;
  --tk-text-primary: #1A1A1A;
  --tk-text-secondary: #666666;
  --tk-text-tertiary: #999999;
  --tk-border: #E6E8EA;
  --tk-shadow: 0 2px 8px rgba(0,0,0,0.08);
  --tk-shadow-lg: 0 8px 24px rgba(0,0,0,0.12);
  --tk-radius: 12px;
  --tk-radius-lg: 16px;
  --tk-radius-xl: 20px;
}

* { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

body {
  background-color: var(--tk-bg);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  color: var(--tk-text-primary);
  line-height: 1.5;
  padding-bottom: 80px;
}

/* Tabs */
.tab-content { display: none; }
.tab-content.active { display: block; animation: fadeIn 0.2s ease-out; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
.nav-active { color: var(--tk-primary) !important; font-weight: 700 !important; }
.nav-admin-active { color: var(--tk-secondary) !important; font-weight: 700 !important; }

/* Inputs & Forms */
input, select, textarea {
  width: 100%; padding: 14px 16px; border: 1px solid var(--tk-border);
  background: var(--tk-card); border-radius: var(--tk-radius);
  font-size: 14px; color: var(--tk-text-primary); transition: 0.2s; outline: none;
}
input:focus, select:focus {
  border-color: var(--tk-primary); box-shadow: 0 0 0 3px rgba(0, 166, 237, 0.15); background: #fff;
}

/* Buttons */
.btn-primary {
  background: linear-gradient(135deg, var(--tk-primary), var(--tk-primary-dark));
  color: white; font-weight: 700; padding: 14px 20px; border-radius: var(--tk-radius);
  border: none; cursor: pointer; transition: 0.2s; font-size: 14px; width: 100%;
  box-shadow: 0 4px 12px rgba(0, 166, 237, 0.3); display: flex; align-items: center; justify-content: center; gap: 6px;
}
.btn-primary:hover { transform: translateY(-1px); box-shadow: 0 6px 16px rgba(0, 166, 237, 0.4); }

.btn-secondary {
  background: linear-gradient(135deg, var(--tk-secondary), var(--tk-secondary-dark));
  color: white; font-weight: 700; padding: 14px 20px; border-radius: var(--tk-radius);
  border: none; cursor: pointer; transition: 0.2s; font-size: 14px; width: 100%;
  box-shadow: 0 4px 12px rgba(255, 107, 53, 0.3);
}

.btn-danger {
  background: var(--tk-danger); color: white; font-weight: 700; padding: 12px 16px;
  border-radius: var(--tk-radius); border: none; cursor: pointer; transition: 0.2s; font-size: 13px; width: auto;
}

.btn-ghost {
  background: transparent; color: var(--tk-text-secondary); font-weight: 500;
  padding: 8px 12px; border-radius: 8px; border: none; cursor: pointer; transition: 0.2s; font-size: 13px;
}
.btn-ghost:hover { background: rgba(0,0,0,0.04); color: var(--tk-text-primary); }

/* Cards */
.card {
  background: var(--tk-card); border-radius: var(--tk-radius-lg); padding: 20px;
  box-shadow: var(--tk-shadow); border: 1px solid var(--tk-border); margin-bottom: 16px;
}
.card-header {
  display: flex; justify-content: space-between; align-items: center;
  padding-bottom: 12px; border-bottom: 1px solid var(--tk-border); margin-bottom: 16px;
}
.card-title { font-size: 15px; font-weight: 700; color: var(--tk-text-primary); margin: 0; }

.card-hero {
  background: linear-gradient(135deg, var(--tk-primary), #0089C9);
  border-radius: var(--tk-radius-xl); padding: 24px; color: white;
  box-shadow: var(--tk-shadow-lg); margin-bottom: 20px; position: relative; overflow: hidden;
}
.card-hero::before {
  content: ''; position: absolute; top: -50%; right: -50%; width: 200px; height: 200px;
  background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%); border-radius: 50%;
}
.card-hero .label { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; opacity: 0.9; margin-bottom: 8px; }
.card-hero .amount { font-size: 32px; font-weight: 800; margin: 4px 0 20px; letter-spacing: -0.5px; }
.card-hero .stats { display: flex; gap: 12px; }
.card-hero .stat-item { flex: 1; background: rgba(255,255,255,0.15); padding: 12px; border-radius: 10px; backdrop-filter: blur(4px); }
.card-hero .stat-label { font-size: 10px; opacity: 0.85; margin-bottom: 4px; text-transform: uppercase; }
.card-hero .stat-value { font-size: 14px; font-weight: 700; }

/* List Items */
.list-item {
  display: flex; align-items: center; padding: 14px 16px; background: var(--tk-card);
  border-radius: var(--tk-radius); border: 1px solid var(--tk-border); margin-bottom: 10px;
  transition: 0.2s; cursor: pointer;
}
.list-item:hover { border-color: var(--tk-primary); box-shadow: 0 2px 8px rgba(0, 166, 237, 0.1); transform: translateX(2px); }

/* Modals */
.modal-overlay {
  position: fixed; inset: 0; background: rgba(0, 0, 0, 0.5); backdrop-filter: blur(4px);
  z-index: 100; display: flex; align-items: center; justify-content: center; padding: 20px;
}
.modal-content {
  background: var(--tk-card); border-radius: var(--tk-radius-xl); padding: 24px;
  width: 100%; max-width: 400px; box-shadow: var(--tk-shadow-lg); animation: modalSlide 0.3s ease;
}
@keyframes modalSlide { from { opacity: 0; transform: translateY(20px) scale(0.98); } to { opacity: 1; transform: translateY(0) scale(1); } }

/* Loader */
.loader-overlay {
  position: fixed; inset: 0; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(8px);
  z-index: 200; display: flex; align-items: center; justify-content: center;
}
.loader-spinner {
  width: 48px; height: 48px; border: 4px solid var(--tk-border); border-top-color: var(--tk-primary);
  border-radius: 50%; animation: spin 0.8s linear infinite; margin-bottom: 16px;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* Header & Nav */
.app-header {
  background: var(--tk-card); border-bottom: 1px solid var(--tk-border); padding: 12px 16px;
  position: sticky; top: 0; z-index: 50; box-shadow: 0 1px 0 var(--tk-border);
}
.bottom-nav {
  position: fixed; bottom: 0; left: 0; right: 0; background: var(--tk-card);
  border-top: 1px solid var(--tk-border); display: flex; justify-content: space-around;
  padding: 8px 0 20px; z-index: 40; box-shadow: 0 -2px 12px rgba(0,0,0,0.04);
}
.bottom-nav.admin { background: #1A1A1A; border-top-color: #333; }
.bottom-nav.admin .nav-item { color: #999; }
.bottom-nav.admin .nav-item.nav-active { color: var(--tk-secondary); }
.nav-item {
  flex: 1; display: flex; flex-direction: column; align-items: center; padding: 8px 4px;
  color: var(--tk-text-tertiary); background: none; border: none; cursor: pointer;
  transition: 0.2s; font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.3px;
}
.nav-item span:first-child { font-size: 22px; margin-bottom: 4px; transition: transform 0.2s; }
.nav-item:hover { color: var(--tk-primary); }
.nav-item.nav-active { color: var(--tk-primary); }
.nav-item.nav-active span:first-child { transform: scale(1.15); }

/* Utilities */
.hidden { display: none !important; }
.badge { display: inline-flex; align-items: center; padding: 4px 10px; border-radius: 20px; font-size: 11px; font-weight: 700; text-transform: uppercase; }
.badge-success { background: rgba(0, 184, 148, 0.12); color: var(--tk-success); }
.badge-warning { background: rgba(253, 203, 110, 0.2); color: #B5860C; }
.badge-danger { background: rgba(255, 71, 87, 0.12); color: var(--tk-danger); }
.badge-code { background: #f1f5f9; color: #475569; border: 1px dashed #94a3b8; font-family: monospace; font-size: 10px; }
